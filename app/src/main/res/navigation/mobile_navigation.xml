<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="edu.cnm.deepdive.buffy.controller.HomeFragment"
        android:label="Search"
        tools:layout="@layout/fragment_home" >

    </fragment>

    <fragment
        android:id="@+id/nav_movie"
        android:name="edu.cnm.deepdive.buffy.controller.MovieFragment"
        android:label="@string/menu_movie"
        tools:layout="@layout/fragment_movie" />

    <fragment
        android:id="@+id/nav_watchlist"
        android:name="edu.cnm.deepdive.buffy.controller.WatchlistFragment"
        android:label="@string/menu_watchlist"
        tools:layout="@layout/fragment_watchlist" />
</navigation>